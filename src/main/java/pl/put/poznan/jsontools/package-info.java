/**
 * This is the root package for the JsonTools application.
 *
 * <p>The app structure is subdivided into three subpackages. The most important from the business
 * point of view is `logic`. It is the home for all processing logic, whereas `app` and `rest` serve
 * rather technical purposes and provide an execution environment.
 */
package pl.put.poznan.jsontools;
